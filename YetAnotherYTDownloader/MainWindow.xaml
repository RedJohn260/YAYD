<Window x:Class="YetAnotherYTDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yet Another YT Downloader" Height="600" Width="800"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown"
        Background="#1E1E1E">
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                BorderThickness="1"
                                BorderBrush="#555555">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A90E2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3570B8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#555555"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBorder"
                                    Width="16" Height="16"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2">
                                <Path x:Name="CheckMark"
                                      Data="M4,8 L8,12 L14,4"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      Visibility="Hidden"/>
                            </Border>
                            <ContentPresenter Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="#4A90E2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#4A90E2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="MainGrid" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- Log window -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label x:Name="ver_label" Content="Version" Grid.Row="0" Grid.Column="0" Foreground="Cyan" FontFamily="Segoe UI" FontSize="16" FontWeight="Bold" Margin="0,0,0,1"/>
        <TextBox x:Name="source_textbox" Grid.Row="1" Grid.Column="0" Margin="0,5" Style="{StaticResource ModernTextBox}" Text="Enter YouTube URL"/>
        <TextBox x:Name="destination_textbox" Grid.Row="2" Grid.Column="0" Margin="0,5" Style="{StaticResource ModernTextBox}" Text="Enter Save Directory"/>
        <Button x:Name="btnBrowse" Content="Browse" Grid.Row="2" Grid.Column="1" Margin="10,5,0,5" Width="100" Style="{StaticResource ModernButton}" Click="BtnBrowse_Click"/>
        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Margin="0,5">
            <CheckBox x:Name="converttomp3_chk" Content="Convert to MP3" Style="{StaticResource ModernCheckBox}" Checked="ConvertToMp3_Checked" Unchecked="ConvertToMp3_Unchecked"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal" Margin="0,5">
            <Button x:Name="btnDownload" Content="Download" Width="100" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="BtnDownload_Click" IsEnabled="{Binding IsDownloadButtonEnabled, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button x:Name="btnStop" Content="Stop" Width="100" Style="{StaticResource ModernButton}" Click="BtnStop_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock x:Name="textBox1" Margin="0,0,20,0" Foreground="Lime" FontFamily="Segoe UI" FontSize="14" Text="Status: None"/>
        </StackPanel>
        <TextBlock x:Name="textBox2" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,1" Foreground="OrangeRed" FontFamily="Segoe UI" FontSize="14" Text="Title: None"/>
        <RichTextBox x:Name="logTextBox" Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,1" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Background="#2D2D2D" Foreground="White" FontFamily="Consolas" FontSize="12" BorderBrush="#555555" BorderThickness="1">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>
    </Grid>
</Window>